const NotFound = () => {
	// -------------------------------------------------
	// Render
	// -------------------------------------------------

	return (
		<div>Not found page</div>
	);
};

export default NotFound;
